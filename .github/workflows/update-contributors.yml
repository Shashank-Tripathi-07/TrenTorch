name: '📋 Update Contributors Table'

# Recomputes CONTRIBUTORS.md (issues raised, PRs raised, PRs merged per
# person) from the real GitHub API whenever an issue or PR is opened, or a
# PR closes. Opens a PR with the change and stops there -- it does not
# merge it. main requires PRs for everyone except repo admins, and this
# bot isn't exempt from that any more than a human contributor is; a
# maintainer reviews and merges like any other PR.

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened, closed]
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write
  issues: read

jobs:
  update-contributors:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run update script
        id: update
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 .github/scripts/update_contributors.py

      - name: Check for changes
        id: diff
        run: |
          if git diff --quiet -- CONTRIBUTORS.md; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Open update PR (no auto-merge)
        if: steps.diff.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BRANCH="update-contributors-$(date +%s)"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "$BRANCH"
          git add CONTRIBUTORS.md
          git commit -m "Update CONTRIBUTORS.md"
          git push origin "$BRANCH"
          gh pr create --title "Update CONTRIBUTORS.md" \
            --body "Automated update from real issue/PR activity. See .github/workflows/update-contributors.yml. Needs a maintainer to review and merge, this bot does not merge its own PRs." \
            --base main --head "$BRANCH"
